const {sb,json}=require('./_store');
module.exports=async(req,res)=>{try{if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):req.body||{};if(!b.id||!b.name||!b.phone||!b.city)return json(res,400,{error:'Required fields missing'});const row={id:b.id,name:String(b.name).slice(0,100),phone:String(b.phone).slice(0,40),city:String(b.city).slice(0,80),business:String(b.business||'').slice(0,200),capacity:String(b.capacity||'').slice(0,200),status:'pending',created_at:b.date||new Date().toISOString()};const rows=await sb('zico_distributors',{method:'POST',body:JSON.stringify(row)});return json(res,201,{ok:true,item:{id:row.id,status:'pending'}});}catch(e){return json(res,500,{error:e.message});}};
